To do list :

[Backend]
signup
login
logout
get total messages for board
get board messages by platform
save message to board
edit board message
delete board message
getMainGamesInfo[index screen]
getPlatformGenreSearchGames

[Done till here]
-------------------------------- 
[Frontend]
[HomePage](index getMaingames) [DONE]
NAVBAR needs to be customized. [Done]
*******working on Navbar & Login.tsx --navbar needs proper routing depending on auth status and show & hide sign up sign in logout button properly.******
Signup.tsx(FORM)
Search By Platform & Genre .tsx(should be DROPDOWN   And / OR Search) 
----------------------------------------
============
2025/Jun/25
Front for Board Detail should be done, now variables are all set already, just need the rendering.
after getting the seeing the single board message,
delete, edit, (both needs password check)
write button should be set and functioning.

=============
2025 / Jun / 30
Board Functionality added with password check. (EDIT, DELETE)[done]
POST Mode is almost done. just need to match the front input and my backend API.
[Done till here]

After finishing the post mode,
 finally it's time to start with live chat..but backend not done yet 

------------

//BUG
1. when signing up // if users enter wrong input, the input is gone with error message.
let's leave the wrong input as is, for convenience.

2. In Search Games, PS5 games do not exist.

3. 










And Frontend Routes need to be set.


----------------------------------------

*Front - haven't started yet. but soon.
Index - HomePage (Carousel / Animation / Recommendation)
Hamburger Button for? NavBar (Game Search / Board / Game Review / FindPartners / AdminBoard(?))

Each Game card has review? --> make a model for this db, 
//each game with its own unque gameId..hmm has a rating field(i'm gonna make)
which is array, and users can submit their rating for a specific game and add their scores to the array.
//after that, if I calculate the average and round and render as stars.(plan for now)









